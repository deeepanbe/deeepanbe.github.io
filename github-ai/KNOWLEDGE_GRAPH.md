# DJ Repository Knowledge Graph

Core nodes:
Repository → File → Symbol → Dependency → Test → Commit → Issue → Pull Request.

Example relationship:
`repository --contains--> file --defines--> function --covered_by--> test`

The graph lets DJ answer portfolio-level engineering questions and select evidence before generating changes.
