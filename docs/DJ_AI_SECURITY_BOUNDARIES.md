# DJ AI Security Boundaries

DJ AI can autonomously retrieve approved information, compare models, run evaluations, detect regressions, recommend corrections, and prepare versioned changes.

It must not expose credentials, bypass authorization, grant itself permissions, disable security controls, ingest arbitrary internet content as trusted instructions, or deploy uncontrolled self-modifying production code.
