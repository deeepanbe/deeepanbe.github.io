# DJ AI Multimodal

Unify text, images, PDFs, documents, spreadsheets, charts, audio, and generated media behind capability adapters.

Each adapter declares supported inputs/outputs, limits, cost, privacy requirements, and failure behavior. The orchestrator chooses capabilities per task and never claims unsupported modality support.
