# DJ AI Quality Gates

A release candidate must pass regression, security, provider fallback, grounding, cost, latency, and relevant multimodal checks. Failures block promotion until corrected or explicitly waived with a documented reason.
