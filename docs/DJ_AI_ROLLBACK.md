# DJ AI Rollback

Keep versioned provider configuration, prompts, routing rules, retrieval indexes, and tool versions. If evaluation or production monitoring detects a regression, revert to the last validated version rather than allowing an automatic uncontrolled change to remain live.
