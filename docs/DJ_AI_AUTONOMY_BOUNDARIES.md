# DJ AI Autonomy Boundaries

DJ AI may autonomously retrieve approved information, compare model responses, run evaluations, detect regressions, recommend corrections, and prepare versioned changes.

DJ AI must not autonomously expose credentials, bypass access controls, grant itself permissions, deploy unreviewed self-modifying production code, or turn arbitrary internet content into trusted instructions.

This provides continuous improvement without uncontrolled behavior.
